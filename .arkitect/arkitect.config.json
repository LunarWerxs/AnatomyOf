{
  "version": 1,
  "project": {
    "name": "anatomyof"
  },
  "checks": {
    "supply-chain-posture-declared": {
      "$reason": "This repository's bun project lives in `app/`, not at the root. Bun reads bunfig.toml from the directory the install runs in and never walks up, so `app/bunfig.toml` is the file that actually governs `bun install` here - a root bunfig.toml would be inert and declaring one would make this check green without making the install any safer. The check resolves a single `bunfigPath` against the repo root and defaults it to the root file, so this points it at the real declaration. The cooldown itself is not weakened: `app/bunfig.toml` is measured exactly as a root one would be.",
      "bunfigPath": "app/bunfig.toml"
    }
  }
}
